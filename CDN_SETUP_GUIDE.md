# 阿里云 CDN 配置指南（解决强制下载 + 开启 HTTPS）

## 目标

- 解决 OSS 强制下载问题
- 开启 HTTPS 安全访问
- 加速网站访问速度
- 使用自定义域名 `www.bigsmaller.com`

---

## 第一步：添加 CDN 加速域名

1. 打开 [阿里云 CDN 控制台](https://cdn.console.aliyun.com/)
2. 左侧菜单 → **域名管理**
3. 点击右上角蓝色按钮 **「添加域名」**

### 填写基本信息

| 配置项 | 填写内容 |
|--------|----------|
| **加速域名** | `www.bigsmaller.com` |
| **业务类型** | 静态加速 |
| **加速区域** | 仅中国内地 |

### 配置源站信息

| 配置项 | 填写内容 |
|--------|----------|
| **源站类型** | OSS 域名 |
| **源站地址** | `bigsmaller-com.oss-cn-hangzhou.aliyuncs.com` |
| **端口** | 80 |
| **回源HOST** | 加速域名（默认）|

4. 点击 **「确定」**
5. 等待 1-2 分钟，域名状态变为 **「正常运行」**

---

## 第二步：配置 HTTPS 证书

1. 在 CDN 域名列表中找到 `www.bigsmaller.com`
2. 点击右侧 **「管理」** 或域名本身进入配置页
3. 左侧菜单 → **HTTPS 配置**
4. 点击 **「修改配置」**

### 开启 HTTPS

1. 打开 **「HTTPS 安全加速」** 开关
2. **证书类型** → 选择 **「云盾（SSL）证书中心」**
3. 点击 **「申请免费证书」**
4. 证书类型选择 **「DV 域名级 SSL」** → 点击 **「立即购买」**（免费）
5. 回到证书列表，找到 `www.bigsmaller.com` 的证书
6. 点击 **「验证」** → 选择 **「DNS 验证」**
7. 阿里云会自动添加 DNS 验证记录（因为域名在阿里云）
8. 等待 1-5 分钟，证书状态变为 **「已签发」**

### 回到 CDN 配置

1. 刷新 CDN 配置页面
2. **证书类型** → 选择刚刚申请的证书
3. **TLS 版本** → 选择 **「TLSv1.1 及以上」**
4. **HTTP/2** → 打开开关
5. 点击 **「确定」**

---

## 第三步：配置缓存规则（重要）

1. 左侧菜单 → **缓存配置**
2. 点击 **「添加」**

### 添加规则 1：HTML 文件不缓存

| 配置项 | 值 |
|--------|-----|
| 类型 | 目录 |
| 内容 | `/` |
| 过期时间 | 0 秒 |

> 这条规则确保 index.html 每次都从源站获取最新版本

### 添加规则 2：JS/CSS 缓存 7 天

| 配置项 | 值 |
|--------|-----|
| 类型 | 文件后缀名 |
| 内容 | `js,css` |
| 过期时间 | 7 天 |

### 添加规则 3：JSON 数据不缓存

| 配置项 | 值 |
|--------|-----|
| 类型 | 文件后缀名 |
| 内容 | `json` |
| 过期时间 | 0 秒 |

3. 点击 **「确定」**

---

## 第四步：配置 HTTP 响应头（解决强制下载）

1. 左侧菜单 → **HTTP 头**（或 **自定义 HTTP 响应头**）
2. 点击 **「添加」**

### 添加规则：移除 Content-Disposition

| 配置项 | 值 |
|--------|-----|
| **HTTP 响应头** | Content-Disposition |
| **操作** | 删除 |

3. 点击 **「确定」**

### 添加规则：设置 X-Content-Type-Options

| 配置项 | 值 |
|--------|-----|
| **HTTP 响应头** | X-Content-Type-Options |
| **操作** | 增加 |
| **响应头值** | nosniff |

4. 点击 **「确定」**

---

## 第五步：修改 DNS 解析

1. 打开 [阿里云域名解析控制台](https://dns.console.aliyun.com/)
2. 找到 `bigsmaller.com` → 点击 **「解析」**
3. 找到 **主机记录为 `www`**、**类型为 CNAME** 的记录
4. 点击 **「修改」**

### 修改记录值

| 项目 | 旧值 | 新值 |
|------|------|------|
| **记录值** | `bigsmaller-com.oss-cn-hangzhou.aliyuncs.com` | CDN 提供的 CNAME 地址 |

> CNAME 地址获取方式：CDN 控制台 → 域名管理 → 找到 `www.bigsmaller.com` → 查看 CNAME 值（格式类似：`www.bigsmaller.com.w.kunlunar.com`）

5. 点击 **「确认」**
6. TTL 保持 **10 分钟**

---

## 第六步：验证访问

1. 等待 5-10 分钟（DNS 刷新 + CDN 生效）
2. 打开浏览器访问：`https://www.bigsmaller.com`
3. 应该看到：
   - ✅ 地址栏显示绿色锁（HTTPS 安全）
   - ✅ 正常显示网页（不再下载文件）
   - ✅ 登录页面正常加载

---

## 常见问题

### Q1: 访问显示 403 Forbidden？
- 检查 CDN 回源 HOST 是否设置为加速域名
- 检查 OSS Bucket 权限是否为「公共读」

### Q2: 证书申请失败？
- 确认域名已完成 ICP 备案
- 等待 DNS 验证记录生效（约 5 分钟）
- 重新点击验证

### Q3: 修改后还是下载文件？
- 清除浏览器缓存（Ctrl+Shift+R）
- 检查 CDN 缓存是否已刷新（CDN 控制台 → 刷新预热 → 刷新目录 `/`）

### Q4: 如何刷新 CDN 缓存？
1. CDN 控制台 → **刷新预热**
2. 点击 **「刷新缓存」**
3. 类型选择 **「目录」**
4. 内容填写 `https://www.bigsmaller.com/`
5. 点击 **「确定」**
6. 等待 1-2 分钟

---

## 配置完成后

| 访问方式 | 效果 |
|----------|------|
| `https://www.bigsmaller.com` | ✅ 推荐，HTTPS 安全访问 |
| `http://www.bigsmaller.com` | ✅ 会自动跳转 HTTPS |
| `http://bigsmaller-com.oss-cn-hangzhou.aliyuncs.com` | ⚠️ 仍可能下载（OSS 原生地址）|

---

**配置过程中遇到任何问题，截图发给我，我立刻帮您解决！** 🔧
